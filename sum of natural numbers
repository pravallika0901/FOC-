#include <stdio.h>
int main()
{
    int n,i,sum;
    printf("enter the number to find its sum");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        sum=sum+1;
    }
printf("sum of the natural number upto%d is %d",n,sum);

}
